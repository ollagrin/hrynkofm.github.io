const Loader = ({ title }) => (
  <div className="w-full flex justify-center items-center flex-col">
    <h1 className="font-bold text-2x1 text-white mt-2">Loading...</h1>
  </div>
);

export default Loader;
